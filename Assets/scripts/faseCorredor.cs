using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class faseCorredor : MonoBehaviour {

	// Use this for initialization
	void Start () {
        PlayerPrefs.SetString("Fase", "Corredor");
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
